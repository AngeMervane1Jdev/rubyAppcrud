
<div class="container py-3">
 <div class="card">
   <div class="card-title">
     <h1>Twitte to be Create</h1>
   </div>
   <div class="card-body">
    <%= form_with(model: @post, url: posts_path,local: true) do |form| %>
    <%= form.hidden_field :content %>
    <p>Texte:<%= @post.content %></p>
    <%= form.submit  "Oui" %>
    <%= form.submit "Retour" ,name:'back'%>
  <% end %>
   </div>
 </div>
</div>